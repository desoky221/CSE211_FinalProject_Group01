# CSS Folder

This folder should contain all CSS stylesheets for the EventsX project.

## Expected Files:
- `style.css` - Main stylesheet (referenced by pages in pages/ folder)
- `common.css` - Common styles (referenced by index.html)
- `index.css` - Homepage specific styles
- Additional page-specific CSS files as needed

## Status:
⚠️ **CSS files not yet created** - Placeholder folder created per PDF requirements (Section 4.2)

## Next Steps:
1. Create main stylesheet files
2. Update HTML link references
3. Implement responsive design using Flexbox
4. Validate CSS using W3C CSS Validator

